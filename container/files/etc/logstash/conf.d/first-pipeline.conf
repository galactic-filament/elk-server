input {
  file {
    path => "/var/log/ayy.log"
    start_position => beginning
    ignore_older => 0
    codec => "json"
  }

  beats {
    port => 5044
  }
}

output {
  elasticsearch {}
  stdout {}
}
